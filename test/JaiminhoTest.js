var assert = require('chai').assert,
	Jaiminho = require('../src/Jaiminho');

describe('Jaiminho functions', function() {
	
	before(function(){
		// do anything before tests
	});
	
	after(function(){
		// do anything after tests
	});

	describe('Fake testing', function(){
		it('Should return true', function(){
			assert.isTrue(true);
		});
	});
});